language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
before_install:
  - pip install sympy --use-mirrors
  - pip install numpy --use-mirrors
install:
  - python setup.py install
script:
  - ./run_tests.sh